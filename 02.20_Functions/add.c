/* Defines a function named "add", takes two ints and returns one: */
int add(int x, int y) {
    /* Must define all vars after a left brace: */
    int z = x + y;

    /* Must return an int: */
    return z;
}
